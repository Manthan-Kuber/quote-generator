import styled from "styled-components";

export const Title = styled.h1`
    margin: 5rem 0 14rem 10rem;
    color: #333;
    font:700 3.6rem var(--ff-raleway) ;
`

export const QuoteWrapper = styled.div`
    display: grid;
    gap: 5rem;
    margin-left: 10rem;
`