export interface dataItem {
    _id: string;
    author: string;
    authorSlug: string;
    content: string;
    dataAdded: string;
    dataModified: string;
    length: number;
    tags: Array<string>;
  }
